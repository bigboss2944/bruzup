<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/foundation-sites@6.6.3/dist/css/foundation.min.css" integrity="sha256-ogmFxjqiTMnZhxCqVmcqTvjfe1Y/ec4WaRj/aQPvn+I=" crossorigin="anonymous"/>

        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}

            <link href="{{asset('css/style.css') }}" rel="stylesheet" />


            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}


    </head>
    <body>


    {% set route_name = app.request.attributes.get('_route') %}

    {% block navbar %}
     <div class="top-bar align-top">

            <div class="top-bar-left">

            <a href="{{ path('entreprise_index') }}">
                <img src="{{ asset('images/1logo_bruz_up_noir_transpa.png') }}" width="150" /></a>
            </div>
            <div class="top-bar-right">

                <ul class="vertical medium-horizontal menu">

                    <li><a class="nav-link" href="{{path('entreprise_index')}}">Accueil</a></li>
                    <li><a class="nav-link" href="{{path('association')}}">L'asso</a></li>
                    <li><a class="nav-link" href="{{path('contact')}}">Contact</a></li>


                    {%  if is_granted('ROLE_USER') %}

                        <li><a class="nav-link" href="{{path('user_edit',{'username': app.user.username })}}">Mon profil</a></li>
                        <li><a class="nav-link" href="{{path('app_logout' )}}">Se déconnecter</a></li>

                    {% else %}

                        <li><a class="nav-link" href="{{path('app_login')}}">Connexion</a></li>
                    {% endif %}
                </ul>
            </div>

        </div>

    {% endblock %}



        {% block body %}



        {% endblock %}
    </body>

</html>
